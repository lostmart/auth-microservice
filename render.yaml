services:
  - type: web
    name: auth-microservice
    env: docker
    plan: free
    healthCheckPath: /api/v1/health
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: staging
      - key: DATABASE_PATH
        value: /tmp/demo.db
      - key: SEED_DATABASE
        value: true
      - key: API_KEY
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: JWT_EXPIRES_IN
        value: 24h
